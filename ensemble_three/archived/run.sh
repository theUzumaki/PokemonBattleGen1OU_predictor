#!/bin/bash
# Archived `run.sh` — original script moved to reduce top-level clutter.
# Use `./ensemble_three/run.sh` in the repository root to run the pipeline.

echo "This is an archived helper script. See top-level README for usage." 
